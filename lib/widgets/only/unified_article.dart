import 'package:flutter/cupertino.dart';

class UnifiedArticle extends StatelessWidget {
  const UnifiedArticle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
